<!DOCTYPE html>
<html>
  <head>
    <%- include('../html_head', {title: title}) %>
  </head>
  <body>
    <div id="page">
      <div id="main">
        <%- include('../page_header', {title: title, subtitle: subtitle, menus:menus}) %>
        <div class="container">
          <table class="table">
            <caption>Liste des emprunts</caption>
            <thead>
              <tr>
                <th>Début</th>
                <th>Nom</th>
                <th>Titre</th>
              </tr>
            </thead>
            <tbody>
              <% for(var i=0; i< borrows.length; i++) { %>
                <tr>
                  <td><%= borrows[i].begin_date.toLocaleString() %></td>
                  <td><%= borrows[i].name %></td>
                  <td><%= borrows[i].title %></td>
                </tr>
              <% } // for %>
            </tbody>
          </table>
        </div>
      </div>
    </div>

    <%- include('../page_footer') %>
  </body>
</html>
